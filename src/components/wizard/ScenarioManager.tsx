// src/components/wizard/ScenarioManager.tsx
'use client';

import React from 'react';

// This component is now obsolete. The save functionality is handled automatically
// by persisting the Redux state to localStorage.
// The file is kept to avoid breaking existing imports until a full cleanup.
export default function ScenarioManager() {
  return null;
}
