// src/app/(dashboard)/dashboard/page.tsx
// This page is a hub that redirects to the appropriate user dashboard.
// The actual redirection logic is now handled by the root page.tsx to prevent
// redirection loops within the dashboard layout.
export default function DashboardPage() {
  return null;
}
